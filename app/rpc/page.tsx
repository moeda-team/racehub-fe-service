export default function RpcPage() {
  return (
    <main className="max-w-2xl mx-auto px-4 py-12">
      <h1
        style={{
          fontFamily: "var(--font-display)",
          fontSize: 28,
          fontWeight: 700,
          marginBottom: 8,
        }}
      >
        RPC / Check-in
      </h1>
      <p style={{ color: "var(--color-ink-3)" }}>
        Modul check-in peserta: scan QR atau pencarian manual. (placeholder)
      </p>
    </main>
  );
}
